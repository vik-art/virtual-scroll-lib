import { Settings } from "./interfaces/settings.interface";

export const SETTINGS: Settings = {
  minIndex: 0,
  maxIndex: 100000,
  startIndex: 6,
  itemHeight: 50,
  amount: 10,
  tolerance: 2,
  prefetchedItems: 8
}
