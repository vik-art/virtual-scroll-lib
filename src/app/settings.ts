export const SETTINGS = {
    minIndex: 1,
    maxIndex: 16,
    startIndex: 6,
    itemHeight: 20,
    amount: 5, //the number of items we want to be visible in the viewport
    tolerance: 2 // additional items that will be rendered but invisible to the user
}